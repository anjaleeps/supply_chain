{% extends 'base.html.twig' %}

{% block title %}TruckSchedule{% endblock %}

{% block body %}
    <h1>TruckSchedule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ truck_schedule.id }}</td>
            </tr>
            <tr>
                <th>Start_time</th>
                <td>{{ truck_schedule.startTime ? truck_schedule.startTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End_time</th>
                <td>{{ truck_schedule.endTime ? truck_schedule.endTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ truck_schedule.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('truck_schedule_index') }}">back to list</a>

    <a href="{{ path('truck_schedule_edit', {'id': truck_schedule.id}) }}">edit</a>

    {{ include('truck_schedule/_delete_form.html.twig') }}
{% endblock %}
