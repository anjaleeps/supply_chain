<form method="post" action="{{ path('driver_assistant_delete', {'id': driver_assistant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ driver_assistant.id) }}">
    <button class="btn">Delete</button>
</form>
