{% extends 'base.html.twig' %}

{% block title %}TruckOrder{% endblock %}

{% block body %}
    <h1>TruckOrder</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('truck_order_index') }}">back to list</a>

    <a href="{{ path('truck_order_edit', {'orders': truck_order.orders}) }}">edit</a>

    {{ include('truck_order/_delete_form.html.twig') }}
{% endblock %}
